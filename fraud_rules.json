{
  "source": "watsonx-orchestrate ensemble (retrained on real Confluent data)",
  "total_transactions": 13963,
  "flagged": 616,
  "rules": [
    {
      "rule_name": "PROPOSED_amount_distance_prev_km_is_night",
      "description": "Flag as fraud when amount <= 19 AND distance_prev_km <= 20 AND is_night == 0",
      "condition": "amount <= 19 AND distance_prev_km <= 20 AND is_night == 0",
      "action": "flag_fraud",
      "confidence": 1.0,
      "support": 212
    },
    {
      "rule_name": "PROPOSED_amount_distance_prev_km_is_night",
      "description": "Flag as fraud when amount <= 5 AND distance_prev_km <= 596 AND is_night == 1",
      "condition": "amount <= 5 AND distance_prev_km <= 596 AND is_night == 1",
      "action": "flag_fraud",
      "confidence": 1.0,
      "support": 66
    },
    {
      "rule_name": "PROPOSED_amount_distance_prev_km_is_night",
      "description": "Flag as fraud when amount > 6 AND amount <= 19 AND distance_prev_km <= 596 AND is_night == 1",
      "condition": "amount > 6 AND amount <= 19 AND distance_prev_km <= 596 AND is_night == 1",
      "action": "flag_fraud",
      "confidence": 1.0,
      "support": 65
    },
    {
      "rule_name": "PROPOSED_amount_distance_prev_km",
      "description": "Flag as fraud when amount <= 19 AND distance_prev_km > 2307",
      "condition": "amount <= 19 AND distance_prev_km > 2307",
      "action": "flag_fraud",
      "confidence": 1.0,
      "support": 52
    },
    {
      "rule_name": "PROPOSED_amount_distance_prev_km_vpn",
      "description": "Flag as fraud when amount > 104 AND distance_prev_km > 3406 AND vpn == 1",
      "condition": "amount > 104 AND distance_prev_km > 3406 AND vpn == 1",
      "action": "flag_fraud",
      "confidence": 1.0,
      "support": 25
    }
  ]
}